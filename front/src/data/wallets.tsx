import IMG_TEZOS from '../assets/images/tezos-logo.png'

const wallets: {id: number, name: string, img: string, available: boolean}[] = [
  {
    id: 1,
    name: "Tezos Wallet",
    img: IMG_TEZOS,
    available: true,
  },
  
]

export default wallets;